node_modules/
dist/
.env
.env.local
*.local

# Rust build artifacts (keep src/wasm/pkg committed so CI doesn't need wasm-pack)
wasm-engine/target/

# WASM build stamp (local build-cache file generated by build-wasm-if-changed.sh)
.wasm-build-stamp
